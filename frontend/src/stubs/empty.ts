// Stub for optional dependencies of third-party wallet connectors (Solana/x402
// payment modules pulled in transitively by RainbowKit's Base Account wallet)
// that this EVM-only app never exercises at runtime.
const empty = {};
export default empty;
export {};
